name: Setup Bun and dependencies
description: Install Bun and restore the locked dependency graph

runs:
  using: composite
  steps:
    - name: Set up Bun
      uses: oven-sh/setup-bun@v2

    - name: Install dependencies
      run: bun ci
      shell: bash
